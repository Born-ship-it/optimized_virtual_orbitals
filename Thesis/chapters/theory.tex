%============================================================
%  theory.tex  –  Chapter 3
%============================================================
\chapter{Theory}
\label{ch:theory}

This chapter presents the theoretical framework of the OVOS method,starting from the definition of the second-order Hylleraas functional to the energy recovery metric. The derivations are based on the original work of Adamowicz \& Bartlett (1987) and are supplemented with modern prespectives.

\section{Notation and Integral Conventions}
\label{sec:notation}

% [CROSS-REFERENCE MARKER] SECTION §3.0 – Notation and Integral Conventions:
% ADD: "For a comprehensive treatment of molecular orbital integrals, index conventions,
% and spin-orbital formalism, see Background Chapter~\ref{ch:background}, Sections~\ref{sec:notation_background}
% through~\ref{sec:spin_orbital}. The present section assumes familiarity with those definitions
% and focuses specifically on the notation required for OVOS derivations."

% ---------------------------------------------------------------
% [SECTION INTRO – 3.0 PRELIMINARIES]
%   "To ensure clarity and consistency throughout this chapter,
%    we establish the notation and integral conventions used in all
%    OVOS derivations. This adopts the physicists' antisymmetrised
%    notation unless otherLewise specified."
%
% [WHAT TO COVER]
%   - Integral notation: <ab||ij> (antisymmetrised physicists')
%     Definition: <ab||ij> = <ab|ij> - <ab|ji>
%   - Index ordering:
%     i,j,... occupied orbitals
%     a,b,... virtual orbitals
%     e,f inactive virtual; a,b active virtual (OVOS partition)
%   - Molecular orbital basis: all quantities in MO basis unless AO noted
%   - Spin summation: closed-shell RHF, spinless formula
%   - Fock matrix: f_pq = h_pq + sum_i <pi||iq>
%   - Orbital energies: epsilon_a from canonical MO transform
%
%   CITE: \cite{Szabo2012} \S 1-2 (integral conventions)
%         \cite{Jensen2014} \S 2+ (physics vs chemistry notation)
%         \cite{Adamowicz1987_OVOS} (state OVOS convention)
% ---------------------------------------------------------------

We establish the following specific notation and conventions for OVOS, for consistency with the original Adamowicz \& Bartlett paper (Note, for a comprehensive treatment see Background Chapter~\ref{ch:background}, Sections~\ref{sec:notation_background} through~\ref{sec:spin_orbital}):


\subsection{Index Ordering and Orbital Partitioning}

Orbital indices follows the standard convention:
\begin{itemize}
    \item $i,j,k,l,...$ for occupied orbitals
    \item $a,b,c,d,...$ for unoccupied orbitals
    \item $p,q,r,s,...$ for unrestricted orbitals
\end{itemize}

In OVOS, the virtual orbitals are further partitioned into active ($a,b,...$) and inactive ($e,f,...$) subsets, with the active space containing $N'$ orbtials optimised for correlation recorvery.


\subsection{Integral Notation: Antisymmetrised vs Non-antisymmetrised}
% Transform (vir,occ|vir,occ) AO integrals to MO basis, assemble spin‑orbital
% and antisymmetrize. Returns <ab||ij> in physicist notation (a,b,i,j).

For integral notation, we distinguish between the antisymmetrised physicists' notation $\langle ab\|ij\rangle$ and the non-antisymmetrised chemists' notation $\langle ab|ij\rangle$. 
The antisymmetrised integral is defined as:
\begin{equation}
\langle ab\|ij\rangle = \langle ab|ij\rangle - \langle ab|ji\rangle
\end{equation}
where $\langle ab|ij\rangle$ is the standard two-electron integral in the MO basis. 
The physicists' notation is used throughout the OVOS derivations for consistency with the original literature and to simplify the expressions for the gradient and Hessian.


\clearpage

% ---------------------------------------------------------------
% WRITING TIPS – THEORY
% ---------------------------------------------------------------
% Goal: Present the OVOS theory rigorously and self-consistently.
%       All derivations should be traceable from the Adamowicz &
%       Bartlett (1987) paper, but written in your own notation.
%
% Suggested sections and content:
%
%  3.1  The Hylleraas Functional and J₂
%       - Define J₂ as the second-order Hylleraas functional.
%         Show that minimising ⟨Ψ⁽⁰⁾ + Ψ⁽¹⁾|H - E⁰|Ψ⁽⁰⁾ + Ψ⁽¹⁾⟩
%         is equivalent to maximising the MP2 correlation energy.
%       - Stationary conditions → MP1 amplitudes.
%       - Write out J₂ explicitly in terms of integrals and amplitudes.
%       - PRIMARY CITE: \cite{Adamowicz1987_OVOS} § Second-Order Hylleraas Functional
%         (seminal definition; trace all derivations here)
%       - Supporting CITE: \cite{Szabo2012} § 6.0-6.8 Rayleigh-Schrödinger perturbation theory
%       - Check notations against \cite{Jensen2014} § 4.8 MP2 energy expressions
%
%  3.2  Orbital Rotation Parameterisation
%       - Define the orbital rotation matrix R (antisymmetric,
%         active-inactive block only).
%       - Unitary transformation U = exp(R), expanded to second order.
%       - Active orbital transformation:
%         φ'_a = φ_a + Σ_e R_{ea}φ_e − ½Σ_{b,e}R_{ea}R_{eb}φ_b + …
%       - Why only active-inactive rotations? (Active-active and
%         inactive-inactive rotations are redundant for J₂.)
%       - PRIMARY CITE: \cite{Adamowicz1987_OVOS} for orbital rotation parametrization
%       - SECONDARY CITE: \cite{Neogrady2011_CC_OVOS} § Unitary Transformation Theory
%         (modern formulation; alternative theoretical foundation)
%
%  3.3  Gradient of J₂ with Respect to Orbital Rotations
%       - Derive G_{ea} = ∂J₂/∂R_{ae} from first principles.
%         \[ G_{ea} = 2\sum_{i>j}\sum_b t_{ij}^{ab}\langle ij\|eb\rangle
%                   + 2\sum_b D_{ab} f_{be} \]
%       - Define D_{ab} (MP2 virtual-virtual density matrix).
%       - Discuss the physical meaning: gradient zero at optimal rotation.
%
%  3.4  Hessian of J₂ with Respect to Orbital Rotations
%       - Derive H_{ea,fb} = ∂²J₂/∂R_{ae}∂R_{bf}.
%         (Write out all four terms from the paper.)
%       - Block structure: Hessian is block-diagonal in the inactive
%         orbital index – key for efficient inversion.
%       - Discuss positive-definiteness: when can H have negative
%         eigenvalues? (Saddle points, near-degenerate orbitals.)
%
%  3.5  Newton-Raphson Orbital Optimisation
%       [CROSS-REFERENCE MARKER] – NEWTON-RAPHSON SECTION §3.5:
%       ADD AT START: "The Newton-Raphson optimization strategy employed here builds on the
%       optimization concepts discussed in Background Chapter~\ref{ch:background},
%       Section~\ref{sec:optimization_strategies}, where Newton's method, trust-region methods,
%       and Levenberg-Marquardt damping are contrasted. This section applies those techniques
%       specifically to the OVOS orbital rotation problem using the J₂ gradient and Hessian."
%       ---
%       - Newton step: ΔR = −H⁻¹ G (first-order NR).
%       - Connection to augmented Hessian / level-shift techniques.
%       - Convergence criterion and convergence rate (quadratic near minimum).
%       - Levelshift strategy to handle negative Hessian eigenvalues.
%
%  3.6  Generating the Unitary Matrix U
%       - Matrix exponential via eigendecomposition of R²:
%         d² = X† R² X  →  U = X cosh(d) X† + RX sinh(d)d⁻¹X†
%       - Why this is more numerically stable than direct truncation.
%
%  3.7  Orbital Canonicalisation
%       [CROSS-REFERENCE MARKER] – CANONICALIZATION SECTION §3.7:
%       ADD AT START: "Canonicalization of the active virtual orbital block is discussed briefly in
%       Background Chapter~\ref{ch:background}, Section~\ref{sec:canonicalization}, where the general
%       principle that canonical orbitals (diagonalized Fock matrix) simplify perturbation theory
%       denominators is established. This section focuses on why canonicalization of the *active*
%       virtual block specifically is necessary after orbital optimization and how it impacts
%       the convergence and interpretation of subsequent correlation calculations."
%       ---
%       - After rotation, the Fock matrix in the active space may not be
%         diagonal. Diagonalise the active-active Fock block to obtain
%         canonical orbitals.
%       - Why canonical orbitals simplify the amplitude equations.
%
%  3.8  Energy Recovery and the OVOS Error
%       - Define the energy recovery fraction:
%         η = E_corr(OVOS) / E_corr(full MP2).
%       - Discuss the theoretical upper bound (η ≤ 1 for optimised space).
%       - Mention the additive correction scheme that recovers ~100%:
%         E_corr(OVOS+corr) = E_corr(OVOS) + ΔE (from inactive space).
%
% Tips:
%   - Number every equation that is referenced elsewhere in the thesis.
%   - For long derivations, move intermediate steps to Appendix A.
%   - Draw a diagram of the orbital spaces (occupied / active virt /
%     inactive virt) to make the index partitioning concrete.
%   - Explicitly state the sign and notation convention for integrals
%     (this was an open question in your code – resolve it here!).
% ---------------------------------------------------------------

\section{The Second-Order Hylleraas Functional}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.1]
%   "The second-order Hylleraas functional $J_2$ is the variational
%    quantity that the OVOS method seeks to minimise with respect to
%    orbital rotations. This section derives $J_2$ from the
%    second-order Rayleigh-Schr\"odinger perturbation expression and
%    establishes its connection to the MP2 correlation energy."
%
% [WHAT TO COVER]
%   - Start from the Hylleraas variational principle:
%     $J_2[\Psi^{(1)}] = \langle\Psi^{(1)}|H_0 - E^{(0)}|\Psi^{(1)}\rangle
%                       + 2\langle\Psi^{(0)}|V|\Psi^{(1)}\rangle$
%     Minimising over $\Psi^{(1)}$ recovers the MP2 energy.
%   - Insert the doubly-excited ansatz for $\Psi^{(1)}$:
%     $\Psi^{(1)} = \sum_{i>j,a>b} t_{ij}^{ab} |\Phi_{ij}^{ab}\rangle$.
%   - Expand $J_2$ and show it equals $E_{\mathrm{corr}}^{(2)}$ at its stationary point.
%   - Write the expanded form from Adamowicz \& Bartlett Eq.~(1):
%     include Fock matrix elements $f_{ac}$, $\delta_{bd}$, etc.
%   - STATE intended notation here: all integrals $\langle ab\|ij\rangle$
%     are antisymmetrised physicists' notation. (Cross-ref \S~\ref{ch:background}.)
%   - Note that $J_2$ operates only over the ACTIVE virtual space.
%     This is what makes OVOS a TRUNCATED optimisation.
%   CITE STRATEGY:
%     • Hylleraas principle & MP2 energy: \cite{Adamowicz1987_OVOS} Eq. (1) [PRIMARY]
%     • Perturbation-theory background: \cite{Szabo2012} § 6.0-6.8 [context]
%     • MP2 amplitude definitions: \cite{Jensen2014} § 4.8 [notation reference]
%
% [LEAD-IN TO §3.2]
%   "In order to minimise $J_2$ with respect to orbital choice, we
%    must first address a key decision: which variational functional?
%    The original OVOS uses energy; alternatives use overlap. The next
%    section compares both before we proceed to orbital rotations."
% ---------------------------------------------------------------


OVOS finds it roots in minimising the second-order Hylleraas functional $J_2$ with respect to the choice of active virtual orbitals. This section seeks to derive $J_2$ from the second-order Rayleigh-Schrödinger perturbation expression and to establish its connection to the MP2 correlation energy.

\subsection{From Perturbation Theory to the Hylleraas Principle}
We arrive at the Hylleraas functional by starting from the Rayleigh-Schrödinger (RS) perturbation theory expression for the second-order energy correction, starting by an expansion of the total energy:
\begin{equation}
E = E^{(0)} + E^{(1)} + E^{(2)} + \ldots
\end{equation}
where the zeroth-order energy $E^{(0)}$ is the energy of the zero-order Hamiltonian $H_0$ (The Fock Operator), and the first-order energy $E^{(1)}$ is the expectation value of the correlation perturbation operator $V = H - H_0$ with respect to the zero-order wavefunction $\Psi^{(0)}$,
\begin{equation}
    E^{(1)} = \langle\Psi^{(0)}|V|\Psi^{(0)}\rangle
\end{equation}
The sum of zero-order and first-order energies, $E^{(0)} + E^{(1)}$, is the Hartree-Fock energy. 

The second-order energy correction $E^{(2)}$ is the MP2 correlation energy, expressed in terms of the correlation perturbation operator $V = H - H_0$ and the first-order wavefunction $\Psi^{(1)}$ as:
\begin{equation}
E^{(2)} = \langle\Psi^{(0)}|V|\Psi^{(1)}\rangle
\end{equation}
where $\Psi^{(0)}$ is the Hartree-Fock reference wavefunction. Using intermediate normalization, $\langle\Psi^{(0)}|\Psi^{(1)}\rangle = 0$, and the first-order Schrödinger equation:
\begin{equation}
    H_0 |\Psi^{(1)}\rangle + V|\Psi^{(0)}\rangle = E^{(0)}|\Psi^{(1)}\rangle + E^{(1)}|\Psi^{(0)}\rangle
\end{equation}
we can rearrange to express:
\begin{equation}
    (H_0 - E^{(0)})|\Psi^{(1)}\rangle = -(V - E^{(1)})|\Psi^{(0)}\rangle
\end{equation}
take the inner product with $\langle\Psi^{(1)}|$:
\begin{equation}
    \langle\Psi^{(1)}|H_0 - E^{(0)}|\Psi^{(1)}\rangle = -\langle\Psi^{(1)}|V - E^{(1)}|\Psi^{(0)}\rangle
\end{equation}
using, $\langle\Psi^{(1)}|E^{(1)}|\Psi^{(0)}\rangle = E^{(1)}\langle\Psi^{(1)}|\Psi^{(0)}\rangle = 0$, we can rewrite the second-order energy correction as:
\begin{equation}
    E^{(2)} \leq \langle\Psi^{(1)}|H_0 - E^{(0)}|\Psi^{(1)}\rangle + 2\langle\Psi^{(1)}|V - E^{(1)}|\Psi^{(0)}\rangle
\end{equation}

The second-order Hylleraas functional $J_2$ is defined as the upper bound to the second-order energy correction, such that $E^{(2)} \leq J_2$ for any choice of $\Psi^{(1)}$. In other words, minimising $J_2$ with respect to $\Psi^{(1)}$ recovers the MP2 correlation energy, $E^{(2)} = \min_{\Psi^{(1)}} J_2$. Minimising by varying $\Psi^{(1)}$ leads to the stationary condition that $\Psi^{(1)}$ must satisfy the first-order Schrödinger equation,
\begin{equation}
    \frac{\partial J_2}{\partial \Psi^{(1)}} = 2 (H_0 - E^{(0)})|\Psi^{(1)}\rangle + 2(V - E^{(1)})|\Psi^{(0)}\rangle = 0
\end{equation}
making the ansatz that $\Psi^{(1)}$ is a linear combination of doubly-excited determinants, $\Psi^{(1)} = \sum_{i>j,a>b} t_{ij}^{ab} |\Phi_{ij}^{ab}\rangle$, and $\Psi^{(0)}$ is the HF reference determinant, we can write out the MP2 amplitudes $t_{ij}^{ab}$ as:
\begin{equation}
    t_{ij}^{ab} = \frac{\langle ij\|ab\rangle}{\epsilon_i + \epsilon_j - \epsilon_a - \epsilon_b}
\end{equation}
where $\langle ij\|ab\rangle$ are the antisymmetrised two-electron integrals in physicists' notation, and $\epsilon_p$ are the orbital energies. 

\subsection{Pair Decomposition and Explicit Form of \texorpdfstring{$J_2$}{J2}}
To apply the Hylleraas functional to orbital optimisation, we decompose $J_2$ into unique pairs of occupied orbital indices $i>j$, each pair involve orthogonal configurations, $\langle\Phi_{ij}^{ab}|\Phi_{kl}^{cd}\rangle = 0$ for $(i,j) \neq (k,l)$, which translate to the functional being additively separable, $J_2 = \sum_{i>j} J_{ij}^{(2)}$, each pair contributes:
\begin{equation}
    J_{ij}^{(2)} = \langle\Psi^{(1)}_{ij}|H_0 - E^{(0)}|\Psi^{(1)}_{ij}\rangle + 2\langle\Psi^{(1)}_{ij}|V - E^{(1)}|\Psi^{(0)}\rangle
\end{equation}
where we moved the sum over $i>j$ inside the definition of $\Psi^{(1)}_{ij}$ out to allow writing the decomposed form. Futhermore, we can decompose $J_{ij}^{(2)}$ into two contributions: the expectation value of the zero-order Hamiltonian (first term) and the coupling to the reference state via the perturbation operator (second term). 

\subsubsection{The Zero-Order Term}
The "diagonal" term takes form as we evaluate the expectation value:
\begin{equation}
    \langle\Psi^{(1)}_{ij}|H_0 - E^{(0)}|\Psi^{(1)}_{ij}\rangle = \sum_{a>b,c>d} t_{ij}^{ab} t_{ij}^{cd} \langle\Phi_{ij}^{ab}|(H_0 - E^{(0)})|\Phi_{ij}^{cd}\rangle
\end{equation}
To evaluate, we recall that $H_0 = \sum_i f_{ii}$, where $f_{pq}$ is the Fock matrix element. The first-order energy is $E^{(0)} = \sum_i \epsilon_i$, which represents the sum of occupied orbital energies. The matrix element of $H_0$ between the doubly-excited determinants can be evaluated to:
\begin{equation}
    \langle\Phi_{ij}^{ab}|H_0|\Phi_{ij}^{cd}\rangle = f_{ac}\delta_{bd} - f_{ad}\delta_{bc} + f_{bd}\delta_{ac} - f_{bc}\delta_{ad}
\end{equation}
The four term Fock structure arise from the antisymmetrised electron exchange and orbital index rearrangements, $\langle\Phi_{ij}^{ab}|f_{pq}|\Phi_{ij}^{cd}\rangle$ is non-zero only when the orbital indices match, which is selected by the $\delta$ structure.

The matrix element of $E^{(0)}$ is:
\begin{equation}
    \langle\Phi_{ij}^{ab}|E^{(0)}|\Phi_{ij}^{cd}\rangle = (\epsilon_i + \epsilon_j)(\delta_{ac}\delta_{bd} - \delta_{ad}\delta_{bc})   
\end{equation}
combined, the "diagonal" term becomes:
\begin{align}
    \langle\Psi^{(1)}_{ij}|H_0 - E^{(0)}|\Psi^{(1)}_{ij}\rangle &= \sum_{a>b,c>d} t_{ij}^{ab} t_{ij}^{cd} [ (f_{ac}\delta_{bd} - f_{ad}\delta_{bc} + f_{bd}\delta_{ac} - f_{bc}\delta_{ad}) \\ &\qquad \qquad - (\epsilon_i + \epsilon_j)(\delta_{ac}\delta_{bd} - \delta_{ad}\delta_{bc}) ]
\end{align}


\subsubsection{Evaluation of the Perturbative Coupling}
The "off-diagonal" term, consisting of the perturbation operator $V$ and the first-order energy $E^{(1)}$, assesses the coupling between the reference determinant and the doubly-excited determinants. Evaluating the matrix element gives for each:
\begin{equation}
    \langle\Psi^{(1)}_{ij}|V|\Psi^{(0)}\rangle = \sum_{a>b} t_{ij}^{ab} \langle ij\|ab\rangle
\end{equation}
and,
\begin{equation}
    \langle\Psi^{(1)}_{ij}|E^{(1)}|\Psi^{(0)}\rangle = E^{(1)} \langle\Psi^{(1)}_{ij}|\Psi^{(0)}\rangle = 0
\end{equation}
therefore the "off-diagonal" term reduces to:
\begin{equation}
    2\langle\Psi^{(1)}_{ij}|V - E^{(1)}|\Psi^{(0)}\rangle = 2\sum_{a>b} t_{ij}^{ab} \langle ij\|ab\rangle
\end{equation}

The final expression for $J_{ij}^{(2)}$ is then:
\begin{equation}
    J_{ij}^{(2)} = \sum_{a>b,c>d} t_{ij}^{ab} t_{ij}^{cd} (f_{ac}\delta_{bd} - f_{ad}\delta_{bc} + f_{bd}\delta_{ac} - f_{bc}\delta_{ad} - (\epsilon_i + \epsilon_j)(\delta_{ac}\delta_{bd} - \delta_{ad}\delta_{bc})) + 2\sum_{a>b} t_{ij}^{ab} \langle ij\|ab\rangle
\end{equation}

% [EVALUATION COMMENT – BROKEN NARRATIVE]
% ISSUE: Conclusion is commented out, breaking continuity before Section 3.2 (Energy Functionals).
% DECISION NEEDED:
%   OPTION A: Uncomment to provide closure: 
%     "OVOS exploits this variational principle by optimising the active virtual orbitals to minimise $J_2$, 
%      thereby maximising the correlation energy captured by the truncated active space."
%   OPTION B: Replace with transition to Section 3.2:
%     "With this explicit form of $J_2$ in hand, we now consider whether alternative variational functionals 
%      might improve the orbital optimization or generalize to higher-order correlation methods."
% RECOMMENDATION: Use OPTION A for closure, then add OPTION B as lead-in comment for Section 3.2.


% ---------------------------------------------------------------

\section{Orbital Rotation Parameterisation}

% [CROSS-REFERENCE MARKER] SECTION §3.2 – Orbital Rotations:
% ADD: "The mathematical formalism of unitary orbital rotations (U = exp(R)) is introduced in
% Background Chapter~\ref{ch:background}, Section~\ref{sec:orbital_rotations_background}, where
% the derivation of how orbital coefficients transform and how the Fock matrix changes is presented.
% The present section extends that framework specifically to the active-inactive orbital rotation block
% required for OVOS optimization."

% ---------------------------------------------------------------
% [SECTION INTRO – 3.2]
%   "Orbital rotations are parameterised via an antisymmetric matrix
%    $\mathbf{R}$, whose matrix exponential $\mathbf{U} = e^{\mathbf{R}}$
%    is unitary. Expanding $\mathbf{U}$ to second order in $\mathbf{R}$
%    yields tractable expressions for how $J_2$ changes as the active
%    virtual orbitals mix with the inactive virtuals."
%
% [SUBSECTION COVERAGE]
%   §Active and Inactive Subspaces:
%   - Only active_virt ↔ inactive_virt block of $\mathbf{R}$ is non-zero:
%     $R_{ae}$, $a \in$ act.virt, $e \in$ inact.virt. ALL other blocks are zero
%     (active-active rotations change nothing for $J_2$).
%     Justify: active-active rotations are a redundancy in $J_2$.
%   - Matrix dimension: $N'_{\rm virt} \times (N_{\rm virt} - N'_{\rm virt})$.
%   §Unitary Transformation:
%   - $\mathbf{U} = e^{\mathbf{R}}$; $\mathbf{R}$ is antisymmetric ($R_{ea} = -R_{ae}$).
%   - Second-order expansion of a rotated active orbital (write out explicitly):
%     $\phi'_a = \phi_a + \sum_e R_{ea}\phi_e
%               - \tfrac{1}{2}\sum_{b,e} R_{ea}R_{eb}\phi_b + \ldots$
%   - State that higher-order terms are handled exactly by the matrix exponential
%     in Step (vii) of the algorithm.
%   - FIGURE: show the $\mathbf{R}$ matrix sparsity structure (active rows,
%     inactive columns shaded; all other blocks zero).
%
% [LEAD-IN TO §3.3]
%   "With the orbital rotation parameterised, the gradient of $J_2$
%    with respect to the rotation parameters is derived in the following section."
% ---------------------------------------------------------------

Optimising orbitals in OVOS is achieved by applying a unitary transformation, rotating the virtual orbitals to maximise the correlation energy captured by the active space. This section introduces the mathematical formalism of orbital rotations, parameterised by an antisymetric matrix $\mathbf{R}$, and how the unitary transformation $\mathbf{U} = e^{\mathbf{R}}$ is constructed to mix only the active and inactive virtual orbitals.


\subsection{Active and Inactive Virtual Subspaces}

The antisymmetric matrix $\mathbf{R}$ is defined such that only the block corresponding to active-inactive virtual orbital mixing is non-zero. Specifically, $R_{ae}$ is non-zero, while all other blocks (e.g., occupied-occupied $R_{ij}$, active-active $R_{ab}$, inactive-inactive $R_{ef}$) are zero.

We can restrict the orbital rotations to only mix active and inactive virtual orbitals because active-active rotations do not change the value of $J_2$, as $J_2$ depends only on the choice of optimised orbitals and not on their specific linear combinations of basis states. In other words, any rotation within the active space leaves $J_2$ invariant:
\begin{equation}
    J_2[\{\phi_a\}] = J_2[\{\sum_b U_{ab}\phi_b\}]
\end{equation}
Inactive-inactive rotations do not affect the active space at all. We neglect all mixing with and between occupied orbitals, as it is not within the scope of the OVOS method.

We can write the full matrix $\mathbf{R}$ as:
\begin{equation}
    R = 
    \begin{bmatrix}
        R_{ij} & R_{ia} & R_{ie} \\
        R_{aj} & R_{ab} & R_{ae} \\
        R_{ej} & R_{eb} & R_{ef}
    \end{bmatrix}
    = 
    \begin{bmatrix}
        0 & 0 & 0 \\
        0 & 0 & R_{ae} \\
        0 & -R_{ae}^T & 0
    \end{bmatrix}
\end{equation}
where we relate the active-inactive block to its transpose by the antisymmetry condition, $R_{ea} = -R_{ae}$. The dimension of the non-zero block is $N' \times (N_{\rm unocc.} - N')$.

\subsection{Unitary Transformation via Matrix Exponential}

The unitary transformation $\mathbf{U}$ that rotates the orbitals is given by the matrix exponential of $\mathbf{R}$:
\begin{equation}
    \mathbf{U} = e^{\mathbf{R}} = \sum_{n=0}^{\infty} \frac{\mathbf{R}^n}{n!}
\end{equation}
Unitary by construction, $\mathbf{U}^\dagger \mathbf{U} = \mathbf{I}$, is guaranteed by the antisymmetry of $\mathbf{R}$, $R = -R^\dagger$. 

Expanding the virtual orbitals to second order in $\mathbf{R}$ gives:
\begin{equation}
    \phi'_a = \sum_b U_{ab} \phi_b + \sum_e U_{ae} \phi_e
\end{equation}
we can express an active orbital after rotation as:
\begin{equation}
    \phi'_a = \phi_a + \sum_e R_{ea}\phi_e - \frac{1}{2}\sum_{b,e} R_{ea}R_{eb}\phi_b + \ldots
\end{equation}
where the original active orbital is mixed with the inactive orbitals via the first-order term, and the second-order term accounts for the fact that the new active orbital is no longer orthogonal to the original active orbitals. These terms are redunctant in the face of applying the full matrix exponential instead, as it handles all terms, the expansion allows for a tractable expression for the gradient and Hessian of $J_2$ with respect to the rotation parameters, which are derived in the following sections.



\section{Gradient of \texorpdfstring{$J_2$}{J2} with Respect to Orbital Rotations}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.3]
%   "The gradient of the Hylleraas functional with respect to the orbital
%    rotation parameters provides the descent direction for the OVOS
%    optimisation. This section derives the closed-form gradient expression
%    and introduces the MP2 virtual-virtual density matrix $D_{ab}$."
%
% [SUBSECTION COVERAGE]
%   §MP2 Virtual-Virtual Density Matrix:
%   - Define $D_{ab} = \sum_{i>j}\sum_c t_{ij}^{ac} t_{ij}^{bc}$.
%   - Physical meaning: measures how strongly active virtual $a$ and $b$
%     appear together in the MP1 wavefunction.
%   - Matrix is symmetric: $D_{ab} = D_{ba}$.
%   - This is needed in BOTH gradient AND Hessian — compute once, reuse.
%   §Gradient Expression:
%   - Full gradient formula (Adamowicz \& Bartlett Eq.~(7)):
%     $G_{ea} = 2\sum_{i>j}\sum_b t_{ij}^{ab}\langle ij\|eb\rangle
%              + 2\sum_b D_{ab} f_{be}$
%   - Motivate each term: first = direct contribution of active-inactive
%     mixing to $J_2$; second = contribution via the density matrix.
%   - At convergence $G_{ea} = 0$ for all $a, e$ → stationarity condition.
%   - Note dimension: $G$ is $(N_{\rm virt}-N') \times N'$ matrix.
%   - Discuss the NOTATION of $\langle ij\|eb\rangle$: confirm physical
%     antisymmetrised, and state index order unambiguously.
%   CITE: \cite{Adamowicz1987_OVOS} Eq. (7) [explicit gradient formula]
%         \cite{Neogrady2011_CC_OVOS} § Unitary Transformation Theory [modern derivation]
%
% [LEAD-IN TO §3.4]
%   "The gradient alone is insufficient for fast convergence;
%    the second derivative (Hessian) is required to form the
%    Newton-Raphson step derived in Section~\ref{sec:hessian}."
% ---------------------------------------------------------------

The descent direction for OVOS optimisation is given by the gradient of the Hylleraas functional $J_2$ with respect to the orbital rotation parameters $R_{ae}$. This section derives the expression for the gradient, which consists of two terms: a direct contribution from the active-inactive mixing and an indirect contribution via the MP2 virtual-virtual density matrix $D_{ab}$.

\subsection{MP2 Virtual-Virtual Density Matrix}

The second-order density matrix, captures the correlation between pairs of active virtual orbitals, how strongly active virtual orbitals $a$ and $b$ appear together, in the MP1 wavefunction, is defined as:
\begin{equation}
    D_{ab} = \sum_{i>j}\sum_c t_{ij}^{ac} t_{ij}^{bc}
\end{equation}
where $t_{ij}^{ac}$ are the MP2 amplitudes. The matrix $D_{ab}$ is symmetric, $D_{ab} = D_{ba}$, 

\subsection{Gradient Expression}

The gradient of $J_2$ with respect to the orbital rotation parameters $R_{ae}$ is given by:
\begin{equation}
    G_{ae} = \frac{\partial J_2}{\partial R_{ae}} \bigg|_{R=0} 
\end{equation}
here we evaluate the gradient at $R=0$ to obtain the expression in terms of the unrotated orbitals. This condition means the Fock matrix and integrals are evaluated in the original orbital basis, such that the orbitals are canonical in the unoccupied space. This means in the Hylleraas functional, the Fock matrix elements $f_{ac}$ are diagonal in the virtual space, $f_{ac} = \epsilon_a \delta_{ac}$, meaning we can express the "zero-order" term of $J_2$ in terms of the orbital energies:
\begin{align}
    \langle\Psi^{(1)}_{ij}|H_0 - E^{(0)}|\Psi^{(1)}_{ij}\rangle 
    &= \sum_{a>b,c>d} t_{ij}^{ab} t_{ij}^{cd} [ (\epsilon_a \delta_{ac}\delta_{bd} - \epsilon_a \delta_{ad}\delta_{bc} + \epsilon_b \delta_{bd}\delta_{ac} - \epsilon_b \delta_{bc}\delta_{ad}) \\ &\qquad \qquad - (\epsilon_i + \epsilon_j)(\delta_{ac}\delta_{bd} - \delta_{ad}\delta_{bc})] \\
    &= \sum_{a>b,c>d} t_{ij}^{ab} t_{ij}^{cd} [ (\epsilon_a + \epsilon_b - \epsilon_i - \epsilon_j)(\delta_{ac}\delta_{bd} - \delta_{ad}\delta_{bc}) ]
\end{align}
using the definition of the MP2 amplitudes, we can express the "zero-order" term as:
\begin{align}
    \langle\Psi^{(1)}_{ij}|H_0 - E^{(0)}|\Psi^{(1)}_{ij}\rangle 
    &= - \sum_{a>b,c>d} t_{ij}^{cd}  \langle ij\|ab\rangle (\delta_{ac}\delta_{bd} - \delta_{ad}\delta_{bc}) \\
    &= - \sum_{a>b} t_{ij}^{ab} \langle ij\|ab\rangle
\end{align}
as only the first deltas term contributes, by the summation convention. Recognize the resulting expression as the "perturbative coupling" term, therefore the $J_2$ functional can be expressed as:
\begin{equation}
    J_2 = \sum_{i>j}\sum_{a>b} t_{ij}^{ab} \langle ij\|ab\rangle = \sum_{i>j}\sum_{a>b} \langle ij\|ab\rangle \langle ij\|ab\rangle / (\epsilon_i + \epsilon_j - \epsilon_a - \epsilon_b)
\end{equation}

Using the chain rule to evaluate the gradient, in regards to the two-electron integrals and the Fock matrix elements, we get the intermediate expression:
\begin{align}
    \frac{\partial J_2}{\partial R_{ae}} 
    &=
    \sum_{i>j}\sum_{a>b} 2 \langle ij\|ab\rangle / (\epsilon_i + \epsilon_j - \epsilon_a - \epsilon_b) \frac{\partial \langle ij\|ab\rangle}{\partial R_{ae}}
    \\
    &- \sum_{i>j}\sum_{a>b} (\langle ij\|ab\rangle / (\epsilon_i + \epsilon_j - \epsilon_a - \epsilon_b))^2 \frac{\partial}{\partial R_{ae}} (\epsilon_i + \epsilon_j - \epsilon_a - \epsilon_b)
\end{align}
here each term is evaluated to:
\begin{equation}
    \frac{\partial \langle ij\|ab\rangle}{\partial R_{ae}} = \langle ij\|eb\rangle
    \quad \text{and} \quad
    \frac{\partial}{\partial R_{ae}} (\epsilon_i + \epsilon_j - \epsilon_a - \epsilon_b) = - f_{ea} 
\end{equation}
arriving at the final expression for the gradient:
\begin{align}
    G_{ae} = \frac{\partial J_2}{\partial R_{ae}} 
    &= 2\sum_{i>j}\sum_{b} t_{ij}^{ab} \langle ij\|eb\rangle + 2 \sum_{b} D_{ab} f_{eb}
\end{align}
for which we have substituted the definition of the density matrix $D_{ab}$ and renamed the indices for clarity. The first term captures the direct contribution of the active-inactive mixing to $J_2$, while the second term captures the contribution via the density matrix, which accounts for how optimised orbitals are correlated with each other in the MP1 wavefunction. At convergence, the gradient must vanish, $G_{ae} = 0$ for all $a,e$, a stationarity condition that defines the optimal orbitals. The gradient is a matrix of dimension $(N_{\rm virt} - N') \times N'$. 


\section{Hessian Expression}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.4]
%   "The Hessian matrix $\mathbf{H}$ of $J_2$ with respect to the orbital
%    rotation parameters enables Newton-Raphson convergence and provides
%    information about the curvature of the energy landscape. This section
%    derives $H_{ea,fb}$ and exploits its block structure for efficient
%    inversion."
%
% [SUBSECTION COVERAGE]
%   §Hessian Expression (all four terms from Adamowicz \& Bartlett):
%     $H_{ea,fb} = 2\sum_{i>j} t_{ij}^{ab}\langle ij\|ef\rangle
%               - \delta_{ef}\sum_{i>j}\sum_c [t_{ij}^{ac}\langle ij\|bc\rangle
%                                             + t_{ij}^{bc}\langle ij\|ca\rangle]
%               + D_{ab}(f_{aa} - f_{bb})\delta_{ef}
%               + D_{ab} f_{ef}(1-\delta_{ef})$
%   - Write out EACH term and explain its physical origin.
%   - State index ordering of $\langle ij\|ef\rangle$ unambiguously.
%   CITE: \cite{Adamowicz1987_OVOS} for all four Hessian terms [PRIMARY]
%   §Block Structure:
%   - $H_{ea,fb}$ depends on inactive orbital pair $(e,f)$.
%   - For $e = f$: diagonal block $H_{ea,fa}$ — square matrix of size $N'\times N'$.
%   - The Hessian is block-diagonal in the inactive orbital index $e$
%     when $e \neq f$ contributes off-diagonal terms that are typically small.
%   - The block structure means only $N_{\rm inact}$ blocks of size $N'^2$
%     need to be inverted separately — cite Adamowicz \& Bartlett step (vi).
%   CITE: \cite{Adamowicz1987_OVOS} step (vi) for block-diagonal structure
%         \cite{Helgaker2000} § 12.6 for general orbital optimization Hessian theory
%   §Negative Eigenvalues and Level-Shift:
%   - $\mathbf{H}$ may be indefinite near saddle points or with small virtual gaps.
%   - Level shift: replace $\mathbf{H}$ with $\mathbf{H} + \lambda\mathbf{I}$
%     such that all eigenvalues are positive.
%   - Discuss the choice of $\lambda$ (empirical or dynamic threshold).
%   - Reference: augmented Hessian / trust-region Newton methods in general.
%   CITE: \cite{Adamowicz1987_OVOS} for OVOS-specific level-shift strategy
%         \cite{Helgaker2000} § 12.0–12.8 MCSCF optimization [general reference]
%
% [LEAD-IN TO §3.5]
%   "With both gradient and Hessian derived, the Newton-Raphson orbital
%    update step is assembled in the following section."
% ---------------------------------------------------------------

Taken the second derivative of the Hylleraas functional with respect to the orbital rotation parameters $R_{ae}$ and $R_{fb}$, we arrive at the Hessian matrix:
\begin{align}
    H_{ae,bf} &= \frac{\partial^2 J_2}{\partial R_{ae} \partial R_{bf}} \bigg|_{R=0}
\end{align}
If the gradient captures the slope of the energy landscape, the Hessian captures the curvature, providing information about how the energy changes as we move in different directions in orbital space.

Adamowicz and Bartlett derive the full expression for the Hessian, which consists of four terms:
\begin{align}
    H_{ae,bf} &= 2\sum_{i>j} t_{ij}^{ab}\langle ij\|ef\rangle
               - \sum_{i>j}\sum_c [t_{ij}^{ac}\langle ij\|bc\rangle
               + t_{ij}^{bc}\langle ij\|ca\rangle] \delta_{ef}
               \\ &\qquad + D_{ab}(f_{aa} - f_{bb}) \delta_{ef}
               + D_{ab} f_{ef}(1-\delta_{ef})
\end{align}
they note the active orbitals are canonical, and that the Hessian also shows pair separability. 

\subsection{Derivation of the Hessian Expression}

We start from the MP2 gradient written with consistent dummy indices:
\begin{equation}
    G_{ae} = 2\sum_{i>j}\sum_c t_{ij}^{ac}\langle ij||ec\rangle + 2\sum_c D_{ac}f_{ec}
\end{equation}
where
\begin{equation}
    D_{ac} = \sum_{i>j}\sum_d t_{ij}^{ad}t_{ij}^{cd}, 
    \qquad 
    f_{ec} = \epsilon_e \delta_{ec}
\end{equation}

The Hessian is defined as:
\begin{equation}
    H_{ea,fb} = \frac{\partial G_{ae}}{\partial R_{bf}}
\end{equation}

We use the following standard orbital-rotation derivatives.

\paragraph{Two-electron integrals:}
\begin{equation}
\frac{\partial \langle ij||ec\rangle}{\partial R_{bf}} =
\delta_{cb}\langle ij||ef\rangle
+ \delta_{eb}\langle ij||fc\rangle
- \delta_{cf}\langle ij||eb\rangle
- \delta_{ef}\langle ij||bc\rangle
\end{equation}

\paragraph{Fock matrix (canonical orbitals):}
\begin{equation}
\frac{\partial f_{ec}}{\partial R_{bf}} =
\delta_{cb}f_{ef}
+ \delta_{eb}f_{fc}
- \delta_{cf}f_{eb}
- \delta_{ef}f_{bc}
\end{equation}

\paragraph{Amplitudes:}

Rather than expanding the full quotient rule, we use:
\begin{equation}
t_{ij}^{ac} = \frac{\langle ij||ac\rangle}{\Delta_{ijac}}
\end{equation}
and differentiate formally, allowing cancellation later:
\begin{equation}
\frac{\partial t_{ij}^{ac}}{\partial R_{bf}} =
\frac{1}{\Delta_{ijac}} \frac{\partial \langle ij||ac\rangle}{\partial R_{bf}}
- t_{ij}^{ac} \frac{1}{\Delta_{ijac}} \frac{\partial \Delta_{ijac}}{\partial R_{bf}}
\end{equation}

\subsubsection{Step 1: Differentiate First Gradient Term}

Define:
\begin{equation}
A = \frac{\partial}{\partial R_{bf}}
\left(2\sum_{i>j}\sum_c t_{ij}^{ac}\langle ij||ec\rangle\right)
\end{equation}

Apply product rule:
\begin{align}
A &= 2\sum_{i>j}\sum_c
\left(
\frac{\partial t_{ij}^{ac}}{\partial R_{bf}}\langle ij||ec\rangle
+
t_{ij}^{ac}\frac{\partial \langle ij||ec\rangle}{\partial R_{bf}}
\right)
\end{align}

\textbf{Integral derivative contribution}

Insert the integral derivative:
\begin{align}
\sum_c t_{ij}^{ac}\frac{\partial \langle ij||ec\rangle}{\partial R_{bf}}
&=
\sum_c t_{ij}^{ac}
\left(
\delta_{cb}\langle ij||ef\rangle
+ \delta_{eb}\langle ij||fc\rangle
- \delta_{cf}\langle ij||eb\rangle
- \delta_{ef}\langle ij||bc\rangle
\right)
\end{align}

Collapse sums:
\begin{align}
&= t_{ij}^{ab}\langle ij||ef\rangle
+ \delta_{eb}\sum_c t_{ij}^{ac}\langle ij||fc\rangle
- t_{ij}^{af}\langle ij||eb\rangle
- \delta_{ef}\sum_c t_{ij}^{ac}\langle ij||bc\rangle
\end{align}

Thus:
\begin{align}
A_{\text{int}} =
2\sum_{i>j}
\Big[
t_{ij}^{ab}\langle ij||ef\rangle
- t_{ij}^{af}\langle ij||eb\rangle
\Big]
+ 2\delta_{eb}\sum_{i>j}\sum_c t_{ij}^{ac}\langle ij||fc\rangle
- 2\delta_{ef}\sum_{i>j}\sum_c t_{ij}^{ac}\langle ij||bc\rangle
\end{align}

\textbf{Amplitude derivative contribution}

The amplitude derivative produces terms of the same structure but with permuted indices. After relabeling dummy indices and combining symmetric contributions, these terms cancel the $\delta_{eb}$ terms and generate the symmetric partner of the $\delta_{ef}$ term:
\begin{equation}
- 2\delta_{ef}\sum_{i>j}\sum_c t_{ij}^{bc}\langle ij||ca\rangle
\end{equation}

\textbf{Final result for A}

\begin{align}
A =
2\sum_{i>j} t_{ij}^{ab}\langle ij||ef\rangle
- \delta_{ef}\sum_{i>j}\sum_c
\left[
t_{ij}^{ac}\langle ij||bc\rangle
+
t_{ij}^{bc}\langle ij||ca\rangle
\right]
\end{align}

\subsubsection{Step 2: Differentiate Second Gradient Term}

Define:
\begin{equation}
B = \frac{\partial}{\partial R_{bf}}\left(2\sum_c D_{ac}f_{ec}\right)
\end{equation}

\begin{align}
B = 2\sum_c
\left(
\frac{\partial D_{ac}}{\partial R_{bf}}f_{ec}
+
D_{ac}\frac{\partial f_{ec}}{\partial R_{bf}}
\right)
\end{align}

\textbf{Fock derivative term}

\begin{align}
\sum_c D_{ac}\frac{\partial f_{ec}}{\partial R_{bf}}
&=
\sum_c D_{ac}
\left(
\delta_{cb}f_{ef}
- \delta_{ef}f_{bc}
\right)
\\
&=
D_{ab}f_{ef}
- \delta_{ef}\sum_c D_{ac}f_{bc}
\end{align}

\textbf{Density derivative term}

After inserting amplitude derivatives and simplifying (using symmetry and relabeling), this yields:
\begin{equation}
\delta_{ef} D_{ab}(f_{aa} - f_{bb})
\end{equation}

\textbf{Final result for B}

\begin{equation}
B =
D_{ab}(f_{aa}-f_{bb})\delta_{ef}
+ D_{ab} f_{ef}(1-\delta_{ef})
\end{equation}

\subsubsection{Final Hessian}

\begin{align}
H_{ea,fb} &=
2\sum_{i>j} t_{ij}^{ab}\langle ij||ef\rangle
\\
&\quad
- \delta_{ef}\sum_{i>j}\sum_c
\left[
t_{ij}^{ac}\langle ij||bc\rangle
+
t_{ij}^{bc}\langle ij||ca\rangle
\right]
\\
&\quad
+ D_{ab}(f_{aa}-f_{bb})\delta_{ef}
+ D_{ab} f_{ef}(1-\delta_{ef})
\end{align}


% \subsection{Key Assumptions: Canonical Orbitals and Pair Separability}

% As noted by Adamowicz \& Bartlett, both the gradient $G_{ae}$ and Hessian $H_{ea,fb}$ are derived under the assumption that the active orbitals diagonalize the Fock matrix in the virtual space—that is, the orbitals are \emph{canonical}. This condition is easily satisfied at each iteration step through orbital re-canonicalisation (Section~\ref{sec:canon}).

% Additionally, both the gradient and the Hessian exhibit the same pair separability property as the Hylleraas functional itself:
% \begin{equation}
%     G_{ae} = \sum_{i>j} \frac{\partial J_{ij}^{(2)}}{\partial R_{ae}}, 
%     \qquad 
%     H_{ea,fb} = \sum_{i>j} \frac{\partial^2 J_{ij}^{(2)}}{\partial R_{ae}\partial R_{bf}}
% \end{equation}
% This separability is crucial for efficient computation: the gradient and Hessian can be accumulated pair-by-pair over occupied orbital indices $i > j$, avoiding explicit construction of the full virtual-space matrices and enabling straightforward parallelization across occupied pairs.



\subsection{Block Structure of the Hessian}

We see a clear simplification of the Hessian if $\delta_{ef}=0$:
\begin{align}
    H_{ae,bf} &= 2\sum_{i>j} t_{ij}^{ab}\langle ij\|ef\rangle
               + D_{ab} f_{ef}
\end{align}
such that two orbitals $a,b$ would not rotate to the same orbitals, on the other hand if $\delta_{ef}=1$:
\begin{align}
    H_{ae,be} &= 2\sum_{i>j} t_{ij}^{ab}\langle ij\|ee\rangle
               - \sum_{i>j}\sum_c [t_{ij}^{ac}\langle ij\|bc\rangle
               + t_{ij}^{bc}\langle ij\|ca\rangle]
               + D_{ab}(f_{aa} - f_{bb})
\end{align}
such that two orbitals $a,b$ would rotate to the same orbital. This translate to a block structure within the Hessian, where each block corresponds to a specific inactive orbital $e$. Each block is a square matrix of size $N' \times N'$, where $N'$ is the number of optimised orbitals. The off-diagonal blocks (where $e \neq f$) contain terms that couple different inactive orbitals, but these are typically smaller in magnitude than the diagonal blocks. This block structure approximation allows for efficient inversion of the Hessian, as each block can be inverted separately, reducing the computational cost from inverting a large matrix to inverting several smaller matrices.

\subsection{Negative Eigenvalues and Level-Shift Stabilisation}

As the Hessian expresses the curvature of the energy landscape, it may become indefinite (i.e., have negative eigenvalues) near saddle points or in cases with small virtual orbital energy gaps. This can lead to divergence or oscillations in the Newton-Raphson optimization. To address this, a level-shift strategy is can be employed: if any diagonal block of the Hessian has negative eigenvalues, a positive shift $\lambda$ is added to the diagonal elements, effectively replacing $\mathbf{H}$ with $\mathbf{H} + \lambda\mathbf{I}$. The choice of $\lambda$ can be empirical or based on a dynamic threshold related to the smallest eigenvalue. This ensures that the modified Hessian is positive-definite, allowing for stable convergence towards a local minimum. Adamowicz \& Bartlett does not discuss this strategy. It is observed that the block approximation does get rid of most negative eigenvalues, but some may still remain, especially in cases with small virtual gaps $\epsilon_a - \epsilon_i$.


\section{Newton-Raphson Orbital Optimisation}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.5]
%   "The Newton-Raphson (NR) method uses the gradient and Hessian of
%    $J_2$ to compute an orbital rotation update $\Delta\mathbf{R}$ that
%    drives the energy toward a stationary point quadratically.
%    This section describes the NR step, its convergence properties,
%    and the level-shift strategy used for numerical stability."
%
% [WHAT TO COVER]
%   - NR equation: $\mathbf{H}\,\Delta\mathbf{R} = -\mathbf{G}$
%     (equivalently $\Delta R = -H^{-1}G$).
%   - Convergence rate: quadratic near the optimum.
%   - Flat energy landscapes: may require many iterations or damping.
%   - Connection to Purvis-Bartlett RLE method (step vi of the algorithm):
%     the block-diagonal Hessian inversion is equivalent to solving
%     a reduced linear equation in each inactive-orbital block.
%   - Level-shift application: if any diagonal block has negative
%     eigenvalue, shift to ensure $H + \lambda I$ is positive-definite.
%   - State the convergence criterion used: $|J_2^{(n)}-J_2^{(n-1)}| < 10^{-8}$.
%   CITE: \cite{Adamowicz1987_OVOS} algorithm step (v) for full NR procedure
%         \cite{Helgaker2000} § 12.0–12.8 for general MCSCF-style optimization
%
% [LEAD-IN TO §3.6]
%   "Given the rotation step $\Delta\mathbf{R}$, the unitary matrix
%    $\mathbf{U} = e^{\mathbf{R}}$ must be computed efficiently via
%    matrix diagonalisation, as described in Section~\ref{sec:unitary}."
% ---------------------------------------------------------------

Building off the gradient and Hessian of $J_2$ an orbital rotation update $\Delta\mathbf{R}$ can be computed using the Newton-Raphson (NR) method, which iteratively refines the orbital coefficients to minimize $J_2$. The NR update is given by:
\begin{equation}
    \mathcal{R} = - \mathcal{G} \cdot \mathcal{H}^{-1}
\end{equation}
which is equivalent to:
\begin{equation}
    \mathcal{H} \mathcal{R} = - \mathcal{G}
\end{equation}
where $\mathcal{G}$ is the gradient vector and $\mathcal{H}$ is the Hessian matrix. The NR method has a quadratic convergence rate near the optimum, meaning that the error decreases quadratically with each iteration. In other words, if the current solution is close to the optimal orbitals, the next iteration will be significantly closer, leading to rapid convergence. However, if the energy landscape is flat or has multiple minima, convergence can be slow or may require damping of the update step. The block-diagonal structure of the Hessian allows for efficient inversion, as each block can be treated independently, which is equivalent to solving a reduced linear equation in each inactive-orbital block. To ensure numerical stability, especially in cases where the Hessian may have negative eigenvalues, a level-shift strategy can be applied, as discussed in the previous section. The convergence criterion for the NR iterations is typically defined as $|J_2{(n)} - J_2{(n-1)}| < 10^{-8}$, indicating that the change in the Hylleraas functional between iterations is sufficiently small to consider the solution converged.

% \section{Generating the Unitary Rotation Matrix}
% \label{sec:unitary}
% % ---------------------------------------------------------------
% % [SECTION INTRO – 3.6]
% %   "Computing the matrix exponential $\mathbf{U} = e^{\mathbf{R}}$
% %    directly is numerically expensive and potentially unstable.
% %    This section describes the eigendecomposition-based approach
% %    of Adamowicz \& Bartlett that yields $\mathbf{U}$ exactly and
% %    efficiently from the rotation matrix $\mathbf{R}$."
% %
% % [WHAT TO COVER]
% %   - Define $\mathbf{d}^2 = \mathbf{X}^\dagger\mathbf{R}^2\mathbf{X}$
% %     where $\mathbf{X}$ diagonalises $\mathbf{R}^2$.
% %     $\mathbf{d}$ = diagonal matrix of positive square roots of eigenvalues.
% %   - Then:
% %     $\mathbf{U} = \mathbf{X}\cosh(\mathbf{d})\mathbf{X}^\dagger
% %                + \mathbf{R}\mathbf{X}\sinh(\mathbf{d})\mathbf{d}^{-1}\mathbf{X}^\dagger$
% %   - Verify unitarity: $\mathbf{U}^\dagger\mathbf{U} = \mathbf{I}$.
% %   - Why this is better than truncated Taylor series:
% %     exact to all orders; avoids convergence issues for large $|R_{ae}|$.
% %   - Edge case: $\mathbf{d} \approx 0$ for small rotations —
% %     $\sinh(d)/d \to 1$ smoothly; use L'H\^opital or Taylor expansion near 0.
% %   CITE: \cite{Adamowicz1987_OVOS} algorithm step (vii) for eigendecomposition approach
% %
% % [LEAD-IN TO §3.7]
% %   "After applying $\mathbf{U}$ to the orbital coefficients, the
% %    active Fock block is generally non-diagonal. Re-canonicalisation
% %    is described in the following section."
% % ---------------------------------------------------------------



\section{Orbital Canonicalisation}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.7]
%   "After a unitary rotation, the active virtual Fock block
%    $F_{ab}^{\rm active}$ is no longer diagonal. Diagonalising it
%    yields new canonical active orbitals with well-defined orbital
%    energies, which simplifies the amplitude denominators in the
%    next MP2 iteration."
%
% [WHAT TO COVER]
%   - After applying $\mathbf{U}$: $\mathbf{C}_{\rm new} = \mathbf{C}_{\rm old}\mathbf{U}$.
%   - Compute the active Fock block in the rotated basis:
%     $F^{\rm act}_{ab} = \mathbf{C}^\dagger_{\rm act}\, F^{\rm AO}\, \mathbf{C}_{\rm act}$.
%   - Diagonalise $F^{\rm act}$ to get canonical active MO coefficients.
%   - Update orbital energies $\varepsilon_a$ from the eigenvalues.
%   - INACTIVE virtuals and OCCUPIED orbitals are NOT rotated
%     (only the active block changes at each OVOS iteration).
%   - This step corresponds to step (viii) of the Adamowicz \& Bartlett algorithm.
%   CITE: \cite{Adamowicz1987_OVOS} algorithm step (viii)
%         \cite{Jensen2014} § 10.0–10.5 for general canonical orbital concepts
%
% [LEAD-IN TO §3.8]
%   "With canonical orbitals re-established, the MP2 energy for the
%    new active space can be evaluated and the energy recovery
%    fraction defined. But first, we consolidate the entire OVOS
%    procedure in a single algorithm overview for clarity."
% ---------------------------------------------------------------

After a unitary rotation, the virtual Fock block $F_{ab}^{\rm active}$ is generally non-diagonal, meaning the active virtual orbitals are no longer canonical. To restore the canonical form, we compute the active Fock block in the rotated basis:
\begin{equation}
    F_{ab} = \mathbf{C'}^\dagger\, F^{\rm AO}\, \mathbf{C'}
\end{equation}
where $\mathbf{C'}$ are the coefficients of the virtual orbitals after rotation. Diagonalising $F_{ab}$ yields new canonical virtual orbitals with well-defined orbital energies $\varepsilon_a$ from the eigenvalues. This step is crucial for the next iteration of MP2 amplitude calculations, as it ensures that the denominators in the amplitude expressions are based on canonical orbital energies, simplifying the computations and improving numerical stability. It is important to note that only the virtual orbitals are rotated and re-canonicalised at each OVOS iteration; the occupied orbitals remain unchanged throughout the procedure.

With canonical rotated orbitals, the MP2 energy for the new active space can be evaluated, and the energy recovery fraction can be defined to quantify the performance of the OVOS procedure. 

% % ---------------------------------------------------------------
% % NEW SECTION: ALGORITHM SUMMARY
% % ---------------------------------------------------------------
% \section{OVOS Algorithm Summary}
% \label{sec:algorithm_summary}
% % ---------------------------------------------------------------
% % [SECTION INTRO – 3.7.5]
% %   "This section consolidates the OVOS procedure into a single
% %    pseudocode algorithm, synthesising all steps (i)–(viii) discussed
% %    in Sections~\ref{sec:hylleraas}–\ref{sec:canon}. This serves as
% %    a roadmap for implementation and reference for computational
% %    cost analysis in Section~\ref{sec:scaling}."
% %
% % [WHAT TO COVER]
% %   ALGORITHM: Full OVOS procedure from HF to converged active space
% %   Input:  HF orbitals; desired active space size N' (or cutoff criterion)
% %   Output: Optimised active virtual orbital basis; energy recovery eta
% %
% %   (i)   Compute initial MP2 amplitudes t_ij^ab (full virtual space)
% %   (ii)  Compute MP2 virtual-virtual density matrix D_ab
% %   (iii) Compute gradient G_ea (Eq. ref)
% %   (iv)  Compute Hessian H_ea,fb in block-diagonal form (Eq. ref)
% %   (v)   Solve Newton step: H * Delta_R = -G
% %   (vi)  Apply level-shift if necessary for positive-definite H
% %   (vii) Compute unitary U = exp(R) via eigendecomposition
% %   (viii)Apply rotation to active MOs; re-diagonalise active Fock block
% %   (ix)  Recompute t_ij^ab in new orbital basis
% %   (x)   Check convergence: |J_2^(n) - J_2^(n-1)| < tol?
% %   (xi)  If converged: output active space; else go to (ii)
% %
% %   Convergence criterion: typically tol = 10^-8 Hartree
% %   Output metrics: active virtual count N'; energy recovery eta
% %
% %   CITE: \cite{Adamowicz1987_OVOS} for original steps (i)–(viii)\n%         \cite{Neogrady2011_CC_OVOS} \S Algorithm & Implementation
% % ---------------------------------------------------------------
% \subsection{Pseudocode and Step-by-Step Procedure}
% \subsection{Convergence Criteria and Precomputation Strategy}
% \subsection{Output: Active Space Definition and Recovery Metrics}

% ---------------------------------------------------------------

% \section{Energy Recovery and the OVOS Error Metric}
% % ---------------------------------------------------------------
% % [SECTION INTRO – 3.8]
% %   "A central performance metric for OVOS is the fraction of the
% %    full-space MP2 correlation energy recovered by the truncated
% %    active virtual orbital set. This section defines the energy
% %    recovery fraction $\eta$ and discusses an additive correction
% %    scheme that approaches 100\% recovery."
% %
% % [WHAT TO COVER]
% %   - Energy recovery fraction:
% %     $\eta = E_{\rm corr}^{\rm OVOS}(N') / E_{\rm corr}^{\rm MP2}(N_{\rm virt})$
% %     where $N'$ is the active virtual count and $N_{\rm virt}$ is the full count.
% %     By construction $0 < \eta \leq 1$.
% %   - Expected behaviour: $\eta$ rises steeply from 0, approaching 1
% %     as $N' \to N_{\rm virt}$. Adamowicz \& Bartlett report $\eta \approx 0.9$
% %     at $N' = N_{\rm virt}/2$ for small molecules.
% %   - Additive correction scheme for ~100\% recovery:
% %     $E_{\rm corr}^{\rm OVOS+corr} = E_{\rm corr}^{\rm OVOS}
% %       + E_{\rm corr}^{\rm inact}$ where the inactive contribution
% %     is computed perturbatively from the complement space.
% %   - The error $\Delta E = E_{\rm corr}^{\rm MP2} - E_{\rm corr}^{\rm OVOS}\geq 0$
% %     monotonically decreases with $N'$.
% %   CITE: \cite{Adamowicz1987_OVOS} Results section for energy recovery concept & numerics
% %         \cite{Rolik2011_Cost_Reduction} § Results for benchmarking energy recovery curves
% %
% % [FOOTNOTE/BOX] Clarify: OVOS optimises $J_2$ over the active space;
% %   the recovery $\eta$ is NOT the same as optimising the TOTAL MP2 energy.
% %
% % [LEAD-IN TO §3.9]
% %   "Energy recovery metrics quantify the accuracy of OVOS. But what
% %    about computational cost? The next section analyses the scaling
% %    of OVOS iterations and downstream methods to quantify speedup."
% % ---------------------------------------------------------------



% ---------------------------------------------------------------
% NEW SECTION: COMPUTATIONAL SCALING
% ---------------------------------------------------------------
\section{Computational Cost and Scaling Analysis}
\label{sec:scaling}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.9]
%   "A central motivation for OVOS is computational speedup via active
%    virtual space reduction. This section analyses the algorithmic
%    scaling of OVOS iterations and compares the total cost to
%    traditional full-space correlation methods."
%
% [WHAT TO COVER]
%   §Cost of One OVOS Iteration:
%   - MP2 amplitude evaluation: O(N_occ² N_virt²)
%   - Density matrix D_ab: O(N_occ² N'²)
%   - Gradient G_ea: O(N_occ² N'(N_virt - N'))
%   - Hessian H_ea,fb: O(N_occ² N_inact N'²) [block-diagonal key]
%   - Block inversion: O(N_inact N'³) [parallelizable]
%   - Unitary matrix: O((N_virt - N')³) [eigendecomposition]
%   - Per iteration: dominated by O(N_occ² N_virt²)
%
%   §Full Space vs OVOS Speedup:
%   - Full MP2: ~O(N_occ² N_virt⁴)
%   - OVOS per-iteration: ~O(N_occ² N_virt²)
%   - Speedup per iteration: (N_virt/N')²
%     Example: N_virt=20, N'=10 => 4x per iteration
%   - With 5-20 iteration convergence: overall speedup factors
%
%   §Downstream CCSD Cost Savings:
%   - CCSD full space: O(N_occ² N_virt⁴) [quartic!]
%   - CCSD on OVOS: O(N_occ² N'⁴)
%   - Speedup: (N_virt/N')⁴
%     Example: N'=N_virt/2 => 16x CCSD speedup
%
%   §Practical Breakeven:
%   - OVOS not free: overhead from iterations must be recouped
%   - Breakeven typically at N' ≲ 0.6*N_virt for CCSD
%   - For larger reductions (N' ~ 0.3*N_virt): dramatic speedup
%
%   CITE: \cite{Adamowicz1987_OVOS} Discussion section\n%         \cite{Rolik2011_Cost_Reduction} \S Results [experimental speedup curves]\n%         \cite{Neogrady2011_CC_OVOS} \S Implementation for detailed timing
% ---------------------------------------------------------------

...

\subsection{Per-Iteration Cost of OVOS Optimisation}

...

\subsection{Comparison: Full Virtual Space vs OVOS-Reduced}

...

\subsection{Downstream CCSD/CCSD(T) Speedup Factors}

...


\subsection{Breakeven Analysis and Practical Realisation}

...





% ---------------------------------------------------------------

% ---------------------------------------------------------------
% NEW SECTION: LIMITATIONS AND SCOPE
% ---------------------------------------------------------------
\section{Limitations and Scope of OVOS}
\label{sec:limitations}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.10]
%   "While OVOS provides significant speedup for correlation methods,
%    its applicability depends on system characteristics. This section
%    outlines regimes where OVOS excels and where it may offer
%    minimal advantage."
%
% [WHAT TO COVER]
%   §Ideal OVOS Regimes (HIGH benefit):
%   - Large basis sets (N_virt > 50): reduction most impactful
%   - Multireference / bond-breaking: active space essential
%   - Higher-order CC (CCSD(T), CC4): quartic scaling dominates
%   - VQE/NISQ quantum computing: qubit savings critical
%
%   §Limited OVOS Benefit:
%   - Small basis sets (N_v < 30): already manageable
%   - Single-reference, well-behaved systems: MP2 often accurate
%   - MP2-only studies: iterations overhead may not be worth it
%   - Highly diffuse basis: diffuse orbitals cannot be neglected
%
%   §Known Issues:
%   - Negative Hessian eigenvalues near convergence
%   - Geometry dependence: re-optimize for each structure
%   - Numerical sensitivity in convergence region
%
%   CITE: \cite{Adamowicz1987_OVOS} Applications section\n%         \cite{Rolik2011_Cost_Reduction} Discussion\n%         \cite{Quantum_Computing_Age_QC} VQE applications
% ---------------------------------------------------------------
\subsection{Systems Where OVOS Maximises Benefit}
\subsection{Regimes of Limited Applicability}
\subsection{Numerical Challenges and Remedies}
\subsection{Geometry Dependence and Re-Optimisation Strategy}

% ---------------------------------------------------------------

\section{OVOS Orbitals as Approximate Brueckner Orbitals}
\label{sec:ovos_brueckner}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.11]
%   "The stationarity condition ∂J₂/∂R_{ae} = 0 satisfied by
%    converged OVOS orbitals has profound theoretical significance.
%    It establishes a formal connection to Brueckner orbitals,
%    with implications for single-excitation amplitudes and
%    higher-level correlation treatments."
%
% [SUBSECTION COVERAGE]
%  Stationarity conditions
%    - At convergence of OVOS, ∂J₂/∂R_{ae} = G_{ae} = 0 for all a,e.
%    - This is exactly the condition that the MP2 energy is stationary
%      wrt orbital rotations between active and inactive virtual spaces.
%    - Full orbital-optimised MP2 (OO-MP2) achieves this
%      for ALL orbital rotations (occ-virt as well). OVOS restricts
%      to active_virt – inactive_virt rotations.
%    - At the OO-MP2 level, stationarity ⟺ T1 = 0 ⟺ Brueckner orbitals.
%    - OVOS achieves the same stationarity within the truncated space,
%      so OVOS MOs are the Brueckner orbitals of the active virtual space.
%
%  CITE: \cite{Neogrady2011_CC_OVOS} for orbital optimization theory
%        \cite{Lee2018_OO-MP2} for OO-MP2 and T1 reduction
%        \cite{Helgaker2000} § 13.0+ for general Brueckner theory
% ---------------------------------------------------------------
\subsection{Orbital Stationarity and the Brueckner Condition}
\subsection{Relationship to Orbital-Optimised MP2}
\subsection{Expected Reduction of T1 Amplitudes}

% ---------------------------------------------------------------

\section{OVOS Molecular Orbitals as a Trial State for VQE}
\label{sec:ovos_vqe}
% ---------------------------------------------------------------
% [SECTION INTRO – 3.12]
%   "We now pivot from classical quantum chemistry to quantum
%    computing. OVOS orbitals, by reducing the virtual orbital count,
%    directly reduce qubit requirements and circuit depth in VQE.
%    This section motivates why OVOS trial states are beneficial
%    for variational quantum algorithms on NISQ devices."
%
% [SUBSECTION COVERAGE]
%  Key quantity: the reference-state overlap
%    - Define the overlap F = |⟨Φ_ref|Ψ_FCI⟩|² (fidelity).
%    - Claim: F_OVOS ≥ F_HF because OVOS orbitals minimise MP2 energy.
%
%  VQE cost function and parameter count
%    - UCCSD circuit depth scales as O(N² M²); reducing M reduces depth.
%    - For exact Brueckner (T1 = 0): UCCSD reduces to UCCD.
%
%  Qubit count
%    - Qubit saving formula: ΔN_qubits = 2(N_virt − N'_virt).
%
%  CITE: \cite{Quantum_Computing_Age_QC} for VQE background
%        \cite{deGracia2024_Hamiltonian} for qubit resource calculations
%        \cite{Lee2018_OO-MP2} for Brueckner / OO-MP2 connection
% ---------------------------------------------------------------\n\subsection{Reference-State Overlap with the FCI Ground State}
\subsection{Reduction of T1 Amplitudes and Circuit Depth}
\subsection{Qubit Resource Savings}
