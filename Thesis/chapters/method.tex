%============================================================
%  method.tex  –  Chapter 4
%============================================================
\chapter{Method}
\label{ch:method}

% ---------------------------------------------------------------
% WRITING TIPS – METHOD
% ---------------------------------------------------------------
% Goal: Describe the algorithm and implementation in enough detail
%       that a competent reader could reproduce your results.
%       This chapter bridges Theory ↔ Computational Details.
%
% Suggested sections and content:
%
%  4.1  Algorithm Overview
%       - Present the full OVOS iterative cycle as a numbered list or
%         flowchart (Algorithm environment or TikZ diagram).
%         Map directly to steps (i)–(ix) of Adamowicz & Bartlett.
%       - Keep the overview brief; details go in subsections below.
%
%  4.2  Integral Transformation
%       - AO → MO transformation: C^† h_AO C and C^† F_AO C.
%       - Partial 4-index transformation: (μν|λσ) → ⟨ij|ab⟩ type only.
%       - Explain why only occ-occ-virt-virt integrals are needed at
%         each iteration (efficiency argument).
%       - Chemists' notation input (PySCF) → physicists' notation used
%         in formulas: clarify the conversion explicitly.
%         (This resolves the open question from your code comments!)
%       - Spin-orbital construction: describe the interleaved α/β scheme.
%
%  4.3  Initial Active Space Selection
%       - Step (iii) of Adamowicz & Bartlett: rank virtual orbitals by
%         their contribution to E_corr.
%       - Diagonal + ½ off-diagonal contribution as selection criterion.
%       - In the current implementation this is replaced by sequential
%         spin-orbital indexing – justify/compare.
%
%  4.4  Computing MP1 Amplitudes and MP2 Energy
%       - Formula and indexing with clear spin-orbital labels.
%       - Denominator construction: Δ_{ij}^{ab} = ε_a + ε_b − ε_i − ε_j.
%       - Restricted summation i>j, a>b vs. full summation (factor of 1/4).
%
%  4.5  Gradient and Hessian Assembly
%       - Show exactly which index loops are implemented.
%       - Highlight the block structure used for efficient inversion
%         (one block per inactive orbital e: H_{ea,eb} for all a,b).
%       - Discuss the level-shift strategy for negative eigenvalues:
%         what threshold is used and why.
%
%  4.6  Orbital Rotation and Update
%       - Application of U to MO coefficient matrix C.
%       - Separate treatment of alpha and beta coefficients (UHF).
%       - Re-canonicalisation of the rotated active Fock block.
%
%  4.7  Convergence Criteria and Iteration Control
%       - Primary criterion: |E_corr^(n) − E_corr^(n-1)| < 10⁻⁸ Hartree.
%       - Maximum iterations: state the value used.
%       - Oscillation detection (if implemented).
%
%  4.8  Initialisation Strategies
%       - Default UHF/RHF initialisation.
%       - "prev" strategy: warm start from previous orbital set.
%       - Random unitary rotation sampling:
%         Q,R = np.linalg.qr(random_matrix), N samples, best selected.
%       - Discuss pros/cons of each strategy.
%
% Tips:
%   - Use pseudocode (algorithm2e or algorithmicx) for the main loop.
%   - Include a diagram showing the index spaces and how they map
%     to array dimensions in the code.
%   - If you changed anything from the original Adamowicz & Bartlett
%     algorithm, clearly say so and explain why.
%   - Reference the specific functions in your implementation.
% ---------------------------------------------------------------

\section{Algorithm Overview}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.1]
%   "This section presents the complete OVOS iterative algorithm as
%    a numbered sequence of steps, following the computational strategy
%    of Adamowicz \& Bartlett \cite{Adamowicz1987}. The pseudocode in
%    Algorithm~\ref{alg:ovos} provides an at-a-glance roadmap;
%    each step is elaborated in subsequent sections."%%
% [TIP] Cross-reference each step in the pseudocode to a later section:
%   e.g., "Step 5 (gradient/Hessian) is detailed in §4.5."
%   Readers should be able to use the algorithm box as a navigation tool.
%
% [FIGURE] Full algorithm flowchart (TikZ or draw.io → PDF):
%   Boxes: SCF → Transform integrals → Select active space → [loop start]
%          Compute t, J₂ → Assemble G, H → Solve ΔR = -H⁻¹G
%          → Compute U = exp(R) → Update C → Canonicalise → [convergence?]
%          → [yes] Report E_corr
%   Caption: "Flowchart of the OVOS iterative optimisation algorithm.
%   Steps are numbered to match Algorithm~\ref{alg:ovos}."
%
% [LEAD-IN TO §4.2]
%   "The first computational step — transforming atomic orbital integrals
%    to the molecular orbital basis — is presented in detail in the
%    following section."
% ---------------------------------------------------------------

\begin{algorithm}[H]
\caption{OVOS Iterative Optimisation}
\label{alg:ovos}
\begin{algorithmic}[1]
  \State Perform SCF calculation to obtain initial MO coefficients $\mathbf{C}$
  \State Transform AO integrals to MO basis: $\langle ij|ab\rangle$
  \State Select initial active virtual space
  \Repeat
    \State Compute MP1 amplitudes $t_{ij}^{ab}$ and MP2 energy $J_2$
    \State Assemble gradient $\mathbf{G}$ and Hessian $\mathbf{H}$
    \State Solve Newton-Raphson equation: $\Delta\mathbf{R} = -\mathbf{H}^{-1}\mathbf{G}$
    \State Generate unitary matrix $\mathbf{U} = \exp(\mathbf{R})$
    \State Update MO coefficients: $\mathbf{C} \leftarrow \mathbf{C}\,\mathbf{U}$
    \State Canonicalise active Fock block
    \State Evaluate new $J_2$
  \Until{$|J_2^{(n)} - J_2^{(n-1)}| < \epsilon_{\mathrm{conv}}$}
\end{algorithmic}
\end{algorithm}

\section{Integral Transformation}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.2]
%   "Transforming the two-electron integrals from the AO basis to the
%    MO basis is the most memory-intensive step of the algorithm.
%    Only \emph{occupied-occupied-virtual-virtual} ($\langle ij|ab\rangle$)
%    integrals are required for MP2 amplitudes and the gradient; this
%    partial transformation avoids the full $\mathcal{O}(N^8)$ cost."
%
% [SUBSECTION COVERAGE]
%   §AO-to-MO transformation:
%   - One-electron: $h_{pq}^{\rm MO} = (C^\alpha)^T h^{\rm AO} C^\alpha$ (and β).
%   - Fock matrix: $F_{pq}^{\rm MO} = (C^\sigma)^T F^{\rm AO} C^\sigma$.
%   - Two-electron: formal 4-step half-transformation; in practice use
%     PySCF's \texttt{ao2mo.kernel()} for $\alpha\alpha$, $\beta\beta$, $\alpha\beta$.
%   §Spin-orbital construction:
%   - After spatial transformation, merge $\alpha\alpha$, $\beta\beta$,
%     $\alpha\beta$ blocks into the spin-orbital ERI tensor.
%   - Use the interleaved index mapping from Background §2.5.
%   §Notation convention:
%   - PySCF outputs chemists' $(pq|rs)$; formulas use physicists'
%     antisymmetrised $\langle pq\|rs\rangle = (pr|qs)-(ps|qr)$.
%   - STATE THE CONVERSION explicitly (resolves the code comment open question).
%
% [LEAD-IN TO §4.3]
%   "With integrals available in the MO basis, the initial active
%    virtual subset is selected as described in the following section."
% ---------------------------------------------------------------
\subsection{AO-to-MO Transformation}
\subsection{Spin-Orbital Construction}
\subsection{Notation Convention: Chemists' vs.\ Physicists' Integrals}

\section{Initial Active Space Selection}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.3]
%   "Before the OVOS iteration begins, an initial set of $N'$ active
%    virtual orbitals must be chosen. This section describes the
%    selection strategy used and compares it with the energy-ranking
%    approach proposed by Adamowicz \& Bartlett."
%
% [WHAT TO COVER]
%   - Adamowicz \& Bartlett step (iii): rank virtual orbitals by their
%     individual contribution to $E_{\rm corr}$:
%     diagonal + $\tfrac{1}{2}$ off-diagonal part.
%   - Alternative used here: sequential spin-orbital index selection
%     (first $N'$ virtuals by index, then optimise). Justify/compare.
%   - Discussion: does the initial choice matter for final convergence?
%     (The "random" initialisation study in Results addresses this.)
%   - Connection to natural orbital selection: a natural orbital initial
%     guess may accelerate convergence.
%
% [LEAD-IN TO §4.4]
%   "Once the active space is defined, the MP1 amplitudes and MP2
%    correlation energy are computed as described in the following section."
% ---------------------------------------------------------------

\section{MP1 Amplitudes and MP2 Energy}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.4]
%   "At each iteration, the first-order MP (MP1) amplitudes are
%    recomputed in the current MO basis and used to evaluate the
%    second-order Hylleraas functional $J_2$. This section states the
%    amplitude and energy formulas with explicit spin-orbital index
%    labelling as implemented in the code."
%
% [WHAT TO COVER]
%   - Amplitude formula (USE EXPLICIT NOTATION matching the code):
%     $t_{ij}^{ab} = -\langle ab\|ij\rangle / \Delta_{ij}^{ab}$
%     $\Delta_{ij}^{ab} = \varepsilon_a + \varepsilon_b - \varepsilon_i - \varepsilon_j$
%   - Only $i,j \in $ occupied; $a,b \in $ ACTIVE virtual.
%   - Summation restriction: $i > j$, $a > b$ (vs. full sum with $\tfrac{1}{4}$).
%   - Energy formula: $J_2 = \sum_{i>j,a>b} t_{ij}^{ab}\langle ij\|ab\rangle$.
%   - Storage: $t$ tensor has shape $(n_{\rm occ}, n_{\rm occ}, N', N')$;
%     discuss memory usage for large systems.
%   - Note: active virtuals ONLY appear in $a,b$ — inactive virtuals
%     do not contribute to $J_2$ directly (only via $G, H$).
%
% [LEAD-IN TO §4.5]
%   "The gradient and Hessian matrices required for the Newton-Raphson
%    step are assembled from the amplitudes computed here, as detailed
%    in the following section."
% ---------------------------------------------------------------

\section{Gradient and Hessian Assembly}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.5]
%   "The gradient vector $\mathbf{G}$ and Hessian matrix $\mathbf{H}$
%    of $J_2$ are the key outputs of each OVOS iteration. This section
%    describes how they are computed from the stored amplitudes and
%    integrals, highlighting the block structure that enables efficient
%    inversion."
%
% [SUBSECTION COVERAGE]
%   §Block structure and efficient inversion:
%   - $G_{ea}$: outer loop over $e \in $ inact., inner over $a \in$ act.
%   - $H_{ea,fb}$: outer loops over $(e,f)$ pairs; inner over $(a,b)$.
%   - For $e = f$: diagonal block $\mathbf{H}^{(e)}$ of size $N' \times N'$.
%   - Only these $n_{\rm inact}$ diagonal blocks need to be inverted —
%     NOT the full $(N' n_{\rm inact}) \times (N' n_{\rm inact})$ matrix.
%   - Explain why off-diagonal blocks $(e \neq f)$ are retained or dropped.
%   §Level-shift stabilisation:
%   - If $\min(\lambda_i(\mathbf{H}^{(e)})) < 0$: shift by $|\min| + \epsilon$.
%   - Cite augmented Hessian literature for justification.
%   - State the level-shift threshold actually used in the code.
%
% [LEAD-IN TO §4.6]
%   "With gradient and (shifted) Hessian assembled, the orbital
%    rotation step is computed and applied to the MO coefficients,
%    as described in the following section."
% ---------------------------------------------------------------
\subsection{Block Structure and Efficient Inversion}
\subsection{Level-Shift Stabilisation}

\section{Orbital Rotation and Update}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.6]
%   "The Newton-Raphson step $\Delta\mathbf{R} = -\mathbf{H}^{-1}\mathbf{G}$
%    defines an orbital rotation that is applied to the MO coefficient
%    matrix via the matrix exponential $\mathbf{U} = e^{\Delta\mathbf{R}}$.
%    This section describes the implementation for UHF (separate
%    $\alpha$ and $\beta$ coefficient matrices)."
%
% [WHAT TO COVER]
%   - Form $\Delta\mathbf{R}$ from block-diagonal Hessian inversion.
%   - Compute $\mathbf{U} = \exp(\Delta\mathbf{R})$ via eigendecomposition
%     of $\mathbf{R}^2$ (see Theory \S~\ref{sec:unitary}).
%   - Apply: $C_{\alpha,\rm new} = C_{\alpha,\rm old}\mathbf{U}_\alpha$ (and $\beta$).
%   - The rotation only affects the VIRTUAL block (occupied untouched).
%   - Re-canonicalise the active Fock block (diagonalise) to get new $\varepsilon_a$.
%   - Update the stored orbital energies and MO coefficients for the next iteration.
%
% [LEAD-IN TO §4.7]
%   "After updating the orbitals, the convergence of the energy is
%    assessed; the criteria and stopping rules are defined in the
%    following section."
% ---------------------------------------------------------------

\section{Convergence and Iteration Control}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.7]
%   "The OVOS iteration terminates when the MP2 correlation energy
%    changes by less than a specified threshold between successive
%    iterations. This section states the convergence criterion,
%    the maximum iteration limit, and any oscillation-damping measures."
%
% [WHAT TO COVER]
%   - Primary criterion: $|J_2^{(n)} - J_2^{(n-1)}| < 10^{-8}$~Hartree.
%   - Maximum iterations: state the value (500 or 2500 depending on run).
%   - Oscillatory behaviour: may occur for large active spaces or near-
%     degenerate orbitals. Detection: monitor sign alternation of $\Delta E$.
%   - Convergence rate: quadratic when Newton-Raphson works cleanly;
%     slower when level shifting is applied.
%   - Report iteration counts in the Results chapter.
%
% [TABLE in Results, described here] Convergence summary:
%   Columns: Molecule | Basis | N' | Iterations | $|\Delta E|_{\rm final}$
%
% [LEAD-IN TO §4.8]
%   "The rate and reliability of convergence depend strongly on the
%    initial orbital guess, which is the subject of the following section."
% ---------------------------------------------------------------

\section{Initialisation Strategies}
% ---------------------------------------------------------------
% [SECTION INTRO – 4.8]
%   "The choice of initial MO coefficients can significantly affect
%    convergence speed and the quality of the final orbital set found
%    by the Newton-Raphson iteration. Three distinct strategies are
%    implemented and compared in this work."
%
% [SUBSECTION COVERAGE]
%   §Default SCF initialisation:
%   - Use canonical UHF/RHF MOs directly as starting point.
%   - Fastest; may converge to a local minimum in the orbital space.
%   §Warm-start from previous orbital set:
%   - For a scan over $N'_{\rm virt} = 2, 4, 6, \ldots$:
%     use the converged orbitals from $N'=k$ as start for $N'=k+2$.
%   - Rationale: nearby orbital spaces should have similar optimal MOs.
%   - May improve recovery fraction and reduce iteration count.
%   §Random unitary sampling:
%   - Generate 500 random unitary starting rotations via QR decomposition:
%     $\mathbf{Q}, \mathbf{R} = \texttt{np.linalg.qr}(\text{random matrix})$.
%   - Apply $\mathbf{Q}$ to the virtual orbital block; run OVOS from each.
%   - Select the result with the lowest $J_2$ at convergence.
%   - Rationale: explore the orbital space landscape; avoid local minima.
%   - Cost: $500\times$ more expensive; only feasible for small systems.
%
% [LEAD-IN TO CH.5]
%   "Having described the full OVOS algorithm and its implementation,
%    Chapter~\ref{ch:computational} provides the specific computational
%    settings, molecular systems, and basis sets used to generate
%    the results presented in Chapter~\ref{ch:results}."
% ---------------------------------------------------------------
\subsection{Default SCF Initialisation}
\subsection{Warm-Start from Previous Orbital Set}
\subsection{Random Unitary Sampling}
